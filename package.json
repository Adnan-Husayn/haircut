{
  "name": "haircut",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "probe": "node --env-file=.env --import tsx scripts/probe.ts",
    "oracle": "node --env-file=.env --import tsx scripts/oracle.ts",
    "prebuild": "node -e \"require('fs').copyFileSync('data/roundtrip_log.csv','public/history.csv')\"",
    "predev": "node -e \"require('fs').copyFileSync('data/roundtrip_log.csv','public/history.csv')\""
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.0",
    "buffer": "^6.0.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  }
}
